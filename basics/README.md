# Basics

## Run `app.js`

```bash
node app.js
```

## Run `mongoLearn.js`

1. `cp .env.example .env` and set `MONGODB_URI` in `.env` (never commit `.env`).
2. Install and run:

```bash
npm install
node mongoLearn.js
```
